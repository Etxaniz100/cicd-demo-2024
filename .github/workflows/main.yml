name: Primer workflow

on: push

jobs:
  tareas_hola_mundo:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar mensaje
        run: echo "Hola github Actions (0-0)"
      - name: Bajase codigo
        uses: actions/checkout@v4
      - name: Probar codigo
        run: python3 main.py
